import { Injectable } from "@nestjs/common";import dayjs from "dayjs";import { extend } from "dayjs";import utc from "dayjs/plugin/utc";extend(utc);@Injectable()export class LoggersService {  private formatDate = dayjs().utc(true).format("YYYY-MM-DDTHH:mm:ss+0000");  constructor() {}  /**   * Logger   */  log = (value: unknown, ...rest: unknown[]): void => {    console.log(`[Log - ${this.formatDate.toString()}]`, `${value} ${rest}`);  };  error = (error: unknown, type?: string): void => {    const logType = type ? `[${type}]` : "";    console.error(`[Error - ${logType}${this.formatDate.toString()}]`, `${error}`);  };  warn = (value: unknown, ...rest: unknown[]): void => {    console.log(`[Warn - ${this.formatDate.toString()}]`, `${value} ${rest}`);  };  info = (value: unknown, ...rest: unknown[]): void => {    console.log(`[Info - ${this.formatDate.toString()}]`, `${value} ${rest}`);  };}